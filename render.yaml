services:
  - type: web
    name: kokoro-tts-server
    runtime: docker
    plan: free
    envVars:
      - key: MODEL_DIR
        value: /app/models
      - key: PORT
        value: 8080
    healthCheckPath: /
